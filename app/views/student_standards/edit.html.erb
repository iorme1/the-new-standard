<div class="form-wrapper">
  <%= bootstrap_form_for [@student, @student_standard] do |f| %>
    <%= f.check_box :completed %>
    <%= f.text_area :notes %>
    <%= f.text_area :assignments %>
    <%= f.submit 'Save', class: 'btn btn-success' %>
</div>
<% end %>
