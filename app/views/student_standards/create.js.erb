$('#standard_<%= @standard_id %>').html('<%= escape_javascript(render @student_standard) %>');

swal({
  text: 'Standard Saved',
  timer: 1500,
  type: 'success',
  showConfirmButton: false
}).catch(swal.noop);
