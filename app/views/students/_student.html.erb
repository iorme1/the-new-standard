<%= content_tag(:tr, id: "student_#{student.id}") do %>
  <td><strong><%= student.name %></strong></td>
  <td><%= link_to "View Record", student %></td>
  <td><strong><%= link_to "Remove Student", student, method: :delete, remote: true, class: "btn btn-danger btn-sm" %></strong></td>
<% end %>
